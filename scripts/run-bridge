#!/bin/sh
set -eu
project_dir=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
if [ -x "$project_dir/.venv/bin/python3" ]; then
    exec "$project_dir/.venv/bin/python3" "$project_dir/backend/bridge.py"
fi
exec python3 "$project_dir/backend/bridge.py"
